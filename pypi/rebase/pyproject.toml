[build-system]
requires = ["setuptools>=77", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "rebase"
version = "0.8.1"
description = "Compatibility package for rebase-toolkit"
readme = "README.md"
requires-python = ">=3.12"
license = "Apache-2.0"
authors = [
    { name = "rebase.energy" },
]
dependencies = [
    "rebase-toolkit==0.8.1",
]

[project.urls]
Homepage = "https://github.com/rebase-energy/rebase-toolkit"
Repository = "https://github.com/rebase-energy/rebase-toolkit"

[project.optional-dependencies]
data = [
    "rebase-toolkit[data]==0.8.1",
]
huggingface = [
    "rebase-toolkit[huggingface]==0.8.1",
]
modeling = [
    "rebase-toolkit[modeling]==0.8.1",
]
hillclimb = [
    "rebase-toolkit[hillclimb]==0.8.1",
]
snowflake = [
    "rebase-toolkit[snowflake]==0.8.1",
]
databricks = [
    "rebase-toolkit[databricks]==0.8.1",
]
bigquery = [
    "rebase-toolkit[bigquery]==0.8.1",
]
fabric = [
    "rebase-toolkit[fabric]==0.8.1",
]
sources = [
    "rebase-toolkit[sources]==0.8.1",
]
all = [
    "rebase-toolkit[all]==0.8.1",
]

[tool.setuptools]
packages = []
