# rebase

Compatibility package for `rebase-toolkit`.

Install this package when you want the shorter PyPI name:

```bash
pip install rebase
```

The actual Python package, CLI, and implementation are provided by
`rebase-toolkit`.
