# .rebase

This repository's `dev` branch is integrated by the Rebase ecosystem framework (gen-1):
every push to `dev` is validated for deployability and deployed to the shared dev environment.
Run your own tests before pushing; the integrator runs only the ecosystem integration suite.
Design: distributed-agents/docs/INCREMENT_4_PLAN.md.
